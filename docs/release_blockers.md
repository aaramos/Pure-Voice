# Release Planning Blockers

## Remaining blocker: version bump source conflicts with script constraint

- Source: `script/build_and_run.sh:10`
- Source: `script/build_and_run.sh:23`
- Source: `script/build_and_run.sh:61`
- Finding: This Swift package does not contain a source `Info.plist`. The app bundle `Info.plist` is generated by `script/build_and_run.sh`, where `MARKETING_VERSION="1.0.0"` becomes `CFBundleShortVersionString`.
- Conflict: The sprint asks to bump the app version, but also says not to modify `build_and_run.sh`.
- Question: Should the release allow a small version-only edit to `script/build_and_run.sh`, or should the project add a separate source-controlled version file and update the scripts in a follow-up?

## STT helper path

- Source: `Sources/PureVoiceApp/App/AppState.swift:294`
- Source: `Sources/PureVoiceApp/App/AppState.swift:296`
- Source: `Sources/PureVoiceApp/Resources/stt_helper.py:1`
- Finding: The sprint prompt references `script/stt_helper.py`, but this repo does not contain that file. `AppState` resolves the helper from the app bundle or `Sources/PureVoiceApp/Resources/stt_helper.py`.
- Decision: Resolved. Parakeet work targets `Sources/PureVoiceApp/Resources/stt_helper.py`.

## Existing hotkey implementation and defaults

- Source: `Sources/PureVoiceApp/App/HotKeyService.swift:4`
- Source: `Sources/PureVoiceApp/App/HotKeyService.swift:24`
- Source: `Sources/PureVoiceApp/App/HotKeyService.swift:52`
- Source: `Sources/PureVoiceApp/App/HotKeyService.swift:75`
- Finding: The current app already has `HotKeyService` in the app target, using `NSEvent` `flagsChanged` monitors. The existing gesture is right Command + right Option to start, and right Option alone to stop; it is not Control-Space.
- Decision: Resolved. The old app-layer service was replaced by a core-layer `HotkeyService`, and Push to Record defaults to the existing right Command + right Option chord.

## Persona guardrail storage

- Source: `Sources/PureVoiceCore/Services/PersonaDefaults.swift:7`
- Source: `Sources/PureVoiceCore/Services/PersonaDefaults.swift:22`
- Source: `Sources/PureVoiceCore/Services/PersonaDefaults.swift:33`
- Source: `Sources/PureVoiceCore/Services/SQLiteStore.swift:173`
- Source: `Sources/PureVoiceCore/Services/SQLiteStore.swift:184`
- Finding: The source currently stores the shared no-reasoning guardrail inside every built-in persona prompt and syncs built-in database prompts back to those full strings. The sprint spec wants persona prompts editable separately from an immutable shared guardrail appended at runtime.
- Decision: Resolved. The editable prompt and immutable shared guardrail are now separated, with the guardrail appended at runtime.

## Parakeet model size

- Source: `https://huggingface.co/mlx-community/parakeet-tdt-0.6b-v2`
- Finding: The model card's MLX usage section lists `pip install -U parakeet-mlx` and `parakeet-mlx audio.wav --model mlx-community/parakeet-tdt-0.6b-v2`, and the page lists the MLX model size as 2.47 GB. The sprint spec says the MLX model download is about 600 MB.
- Decision: Resolved. The UI does not show a specific download size.
